# Weakness Areas

Current observations about Daniel's Italian — recurring errors, gaps, interference patterns. Descriptive, not a plan. The forward-looking plan lives in `LEARNING_ROADMAP.md`.

## Drill targets

| # | Weak spot | Notes |
|---|---|---|
| a | 3rd-person-singular essere `è` slipping to `sono` under load | |
| b | Futuro `-care/-gare` h-insertion and `-iare` merge | Still slightly leaky — resurfaced in a time-telling drill: *mangiare → mangieremo* instead of *mangeremo* (kept the stem *-i-* that should drop before *-e/-i* endings). |
| c | **Verb government: direct vs indirect object per verb** (which verb takes bare object vs *a*) | Self-reported hardest area — clitic *mechanics* are solid, the leak is purely *lexical*: knowing a given verb's category. Trap verbs: *telefonare/rispondere/chiedere* → indirect (Sp *llamar/preguntar* mislead); *ringraziare/pagare/aspettare/ascoltare/cercare/guardare* → direct (no *a*); *pensare a/tenere a/contare su* → neither clitic, needs the right preposition. Current slips: *ringraziare* still taken as indirect; *pensare* still reached for *di* instead of *a*; *chiedere* double-object drops the direct *lo* (says only *gli*, should be *glielo*); *contare su* produced as *contare in*. Same pattern now also surfaces in *ne* vs *ci* choice under *pensare a* (topic) vs *pensare di* (opinion). |
| d | **Irregular past participles regularized to `-uto`** | *chiedere → chieduto* (should be *chiesto*), *rispondere → risposta* (should be *risposto*) — *-uto* over-generalized as default. |
| e | *-cia/-gia* plural: consonant-preceded subclass (drop *i*) | *arancia→arancie* instead of *arance* — applied the vowel-preceded retention rule (*camicia→camicie*, correct) to a consonant-preceded case where it should drop. |
| f | Numerals: *milione* construction and thousands spelling | *Mille* used for "a million" with *di* dropped entirely (should be *un milione **di** persone*); *due mille* written as two words instead of *duemila*; *ventitresimo* missing the double-*e* (*tré + esimo → ventitre**e**simo*). Elision (*ventuno, ventotto*) and *cento* (invariable, no article) were solid. |
| g | Dates: dropping the article entirely when there's no explicit "on"-preposition | *Tre ottobre partiamo...* instead of *Il 3 ottobre partiamo...* — correctly saw no separate preposition is needed, overcorrected by dropping the article too. Rest of the date system (one-off vs habitual day + article, *domenica* fem., *il primo* exception, cardinal not ordinal) was solid. |
| h | Idiomatic destination prepositions (*a casa/scuola/letto/teatro* no article, *al cinema/ristorante/mare* with article, *in ufficio/banca/chiesa/montagna* no article) | Closed list, no generative rule — flagged as unclear right after teaching. City/country *a* vs *in* rule itself (and its extension to any motion verb, not just *andare*) was solid. |
| i | *Niente* vs *nessuno* — reaching for *niente* ("nothing") when the referent is a person ("no one") | Last quizzed 2026-07-22: two slips in one quiz (*Non ho invitato niente alla festa* / *Niente mi ha chiamato oggi*, both should be *nessuno*). Negation structure itself (non-drop with preposed negative, *mai* slotting between aux/participle) was solid — the leak is purely lexical selection between the two negative pronouns. |

## Standing interference watchlist

Patterns to flag in any Italian Daniel writes.

**Syntax**
- *a* before direct-object people (a-personal carryover).

**Vocab slips**
- *bono* → *buono*
- *agora mismo* → *adesso* / *ora*
- *cada vez* → *ogni volta*
- *otra vez* → *di nuovo* / *un'altra volta*

**Morphology**
- Plural family members need the article: *i miei fratelli*, not *mio fratelli*.

## How to maintain this file

- Add a row once an error type **recurs** (2+ instances) in quiz or free writing, or Daniel flags it as a real struggle. A single isolated slip — even on brand-new material — is normal noise, not a pattern, and does **not** get a row.
- **Overwrite, don't append.** When a row's state changes, replace its note with the current picture — never stack a new dated paragraph next to the old one. Each row is a snapshot of where things stand *now*, not a log of every re-drill.
- Remove a row entirely once a clean drill confirms internalization — don't leave a "resolved" note behind.
- **Keep the `#` letters sequential (no gaps).** When a row is removed, re-letter every row after it so the column still reads a, b, c, … with no skip.
- **Every row's note carries a "last quizzed" date.** After quizzing a row and it's still weak, overwrite the note with today's date plus a brief bit of nuance on how the error is progressing (narrowing, same shape, new sub-case, etc.) — not just "still weak." If a row already carries today's date, it's done for the day: don't poke for it or re-quiz it again until a later day (see `RULES_LEARNING.md` → multiple-of-3 poke rule).
- Keep it short and clinical. Drill ideas, sequencing, and planned additions belong in `LEARNING_ROADMAP.md`.
